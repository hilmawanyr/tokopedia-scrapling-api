# Tokopedia Review Scraper

## Tech Stack
- Python 3.11
- FastAPI
- Scrapling
- UV

## Installation (no docker) 
```bash
uv sync
```

## Run (no docker) 
```bash
uv run uvicorn main:app --reload --env-file .env
```

## Run (with docker) 
```bash
docker-compose up -d
```
